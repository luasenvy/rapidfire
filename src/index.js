const { RapidFire } = require('./core')

const Interfaces = require('./interfaces')

module.exports = {
  /** @module RapidFire */
  RapidFire,
  /**
   * @module Interfaces
   * @module Interfaces:Controller
   */
  Interfaces,
}
